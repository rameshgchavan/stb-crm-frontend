import { useState } from "react";
import { Form, FormGroup } from "react-bootstrap";

const BillSection = ({ acNo }) => {

    return (
        <FormGroup className="col border shadow rounded p-3">
            
        </FormGroup>
    )
}

export default BillSection